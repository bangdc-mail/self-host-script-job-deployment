name: Scheduled Job Runner

on:
  schedule:
    - cron: "45 * * * *"

jobs:
  run-job:
    runs-on: self-hosted

    concurrency:
      group: scheduled-job
      cancel-in-progress: false

    steps:
      - name: Run container
        run: docker run --rm my-job:latest